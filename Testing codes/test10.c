const int a=+1, b=2, c=1;
const char ch1='Y',ch2='N';

int i,j,tmp,fibo,number;
int array[10];
char word[10;

int compute(){
    int n;
    scanf(tmp);
    n=a+b*c-(tmp+3)*2;
    printf(n);
}

void main()
{
    int choice;
    scanf(choice);
    if(choice<10){
        switch(choice){
          	 case 2: compute();
            default: printf("overflow");
        }
    }
}
